# 2026-04-24 工作记录

## v4.4.2 主面板版本号修正（09:26）
- 问题：主面板标题栏显示 v4.4.0，实际代码已是 v4.4.2
- 修复：主面板 `<span class="ata-hdr-ver">` 和 README.md 版本号同步更新为 v4.4.2
- 已推送至 GitHub（README + CHANGELOG + 脚本本体）

## v4.4.2 主面板版本号自动读取（09:44）
- 修复：主面板版本号从硬编码改为 `GM_info.script.version` 动态读取
- 以后只需改 `@version`，面板自动同步显示，无需两处同步修改

## v4.4.3 暂停/继续功能（09:50）
- 答题中可点击黄色"暂停"按钮，循环用 `while(paused)` 阻塞等待
- 暂停时提交倒计时秒数冻结，`submitRem` 保存剩余秒数，继续后从剩余秒数继续倒计时
- 停止/完成/重置时隐藏暂停按钮；停止时同时 `clearInterval(submitTickId)`
- 新增 `.ata-btn.yellow` CSS 样式

## v4.4.4 主面板收起功能（10:26）
- 标题栏右侧新增 ▼ 按钮，点击收起主体（仅保留标题栏），按钮变 ▲，再次点击展开
- CSS `.ata-panel.collapsed #ata-body` 控制隐藏，内容包裹在 `#ata-body` 容器中

## v4.4.5 倒计时阶段可暂停（10:29）
- 引入 `inCountdown` 标志区分答题阶段和倒计时阶段
- 答题完成后保留暂停按钮（变为橙色"⏸ 暂停倒计时"），倒计时期间可正常暂停/继续
- 暂停时状态栏显示"⏸ 倒计时已暂停（剩余 Ns）"
- 答题中暂停→黄色；倒计时中暂停→橙色；两种模式文案区分
