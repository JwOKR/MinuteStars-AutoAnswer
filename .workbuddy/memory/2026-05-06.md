# 2026-05-06

## MinuteStars v4.5.64 纯云端改造

### 变更内容
- **BUILTIN_DB 清空**：内置题库（~410行，约300条）已移除，改为 `const BUILTIN_DB = {}` 占位
- **版本号**：v4.5.63 → v4.5.64
- **@description 更新**：移除"内置300+题库"，改为"纯云端题库"
- **rebuildCache()**：`{ ...BUILTIN_DB, ...userDB }` → `{ ...userDB }`
- **cloudUpload()** 重写：新增合并逻辑（先拉云端+本地合并，本地优先，再上传）
- **cloudDownload()** 重写：云端完全覆盖本地（纯云端权威来源）
- **cloudImport()** 新增：拉取云端追加到本地（本地已有保留）
- **UI 更新**：
  - 设置页：新增"导入云端"按钮
  - 云同步按钮文字更新（"上传题库（合并）"/"下载题库（覆盖）"）
  - 统计面板：移除内置/自定义筛选，统一显示
  - 饼图：改为单一颜色显示
- **CHANGELOG.md**：新增 v4.5.64 条目
- **README.md**：版本号更新，题库说明更新

### 云端题库三按钮说明
| 按钮 | 行为 |
|------|------|
| ⬆ 上传题库（合并） | 云端+本地合并（本地优先）→ 上传 |
| ⬇ 下载题库（覆盖） | 云端完全替换本地 |
| ☁ 导入云端 | 云端追加到本地（本地已有保留） |
