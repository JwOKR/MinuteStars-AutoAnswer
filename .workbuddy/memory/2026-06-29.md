# 2026-06-29

## v4.8.52 — Excel 导入显示与 Word 导入统一
- 导出按钮文案含条数（和 Word 一样）
- 导出条件基于 debugLog 条数（和 Word 一样）
- 导出回调直接输出 debugLog JSON（和 Word 一样）
- 消息拼接顺序：dupHtml + previewHtml + exportBtn（和 Word 一样）
- 清理了重复 setTimeout 闭合导致的残留代码

## 修复 Excel 导入日志导出按钮不显示 (v4.8.48)
- **问题**：Excel 导入后日志导出按钮仅在 `window.__docxDebugLog` 有内容时显示，某些情况下为空导致按钮消失
- **修复**：
  - 基于 `result.duplicates/errors/preview` 构造导出数据，不依赖 debugLog
  - 导出计数 = debugLog 条数 || duplicates + errors + preview 条数
  - 存储导入结果到 `window.__excelImportResult` 供导出回调使用
  - 导出时优先使用 debugLog 详细日志，回退使用导入结果摘要 JSON

## Gitee 仓库文件云同步 (v4.8.51)
- **背景**：Gitee Gist 只能私有，无法公开分享题库
- **方案**：题库存到 `law-of-order/MinuteStars-AutoAnswer` 仓库文件（tiku.json）
- **关键函数**：
  - `repoRawUrl(filePath)` — 公开 raw 链接（免认证）
  - `repoApiUrl(filePath)` — API 端点（需 Token）
  - `_readRepoFile(filePath)` — 先 raw 后 API 双通道读取
  - `_writeRepoFile(filePath, content, message)` — base64 编码后 PUT
- **分享**：生成时间戳快照 `tiku_share_xxx.json`，返回公开 raw URL
- **导入**：支持粘贴完整 URL（自动识别 http 开头走 fetch）
- `_gistReq` → `_cloudReq`（通用请求函数重命名）
- 配置：`cloudGistId` 保留但不使用，新用 `cloudFilePath`
